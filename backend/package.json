{
  "name": "bisonbytes25-backend",
  "version": "1.0.0",
  "description": "Backend for BisonBytes25 application",
  "main": "server.js",
  "scripts": {
    "start": "source venv/bin/activate && uvicorn app:app --reload & nodemon server.js",
    "dev": "source venv/bin/activate && uvicorn app:app --reload & nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "agora-rtc-sdk-ng": "^4.23.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "json-autocomplete": "^0.1.3",
    "mongoose": "^8.3.5",
    "node-fetch": "^2.6.7",
    "openai": "^4.89.0",
    "socket.io": "^4.7.5",
    "web-audio-analyser": "^2.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
